-- 设置模块：保存默认窗口参数（将来也可用于存档用户自定义设置）

return {
    display = {
        width = 1280,       -- 窗口宽度
        height = 720,       -- 窗口高度
        fullscreen = false, -- 默认不启用全屏显示
        resizable = true,   -- 允许拖动改变窗口大小
        vsync = true        -- 启用垂直同步
    }
}
